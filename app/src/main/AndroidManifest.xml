<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.apkglobal.lyricsdownloader">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/launcher_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/launcher_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LauncherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" />
        <activity android:name=".LatestActivity"
            android:label="Latest"/>
        <activity android:name=".BollywoodActivity"
            android:label="Bollywood"/>
        <activity android:name=".HollywoodActivity"
            android:label="Hollywood"/>
        <activity android:name=".PunjabiActivity"
            android:label="Punjabi"/>
        <activity android:name=".SigninActivity"
            android:label="Registration Form"/>
        <activity android:name=".AboutActivity"
            android:label="About"/>
        <activity android:name=".ContactActivity"
            android:label="Contact"></activity>
    </application>

</manifest>